#!/usr/bin/env node
/**
 * Wrap the CLI execution so that node-sass's aborts are caught and ignored
 */

'use strict';

var exec = require(__dirname + '/../lib/retry-signaled').exec;

exec(__dirname + '/cogwheels-cli', { include_stdin: true });
