/**
 * @class LibSass.Result.Stats
 * @abstract
 */

/**
 * @property {String} entry the path to the scss file, or `data` if the source was not a file.
 */

/**
 * @property {Number} start `Date.now()` before the compilation
 */

/**
 * @property {Number} end `Date.now()` after the compilation
 */

/**
 * @property {Number} duration `end - start`
 */

/**
 * @property {String[]} includedFiles absolute paths to all related scss files in no particular order
 */
